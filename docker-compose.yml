version: '3.8'

services:
  slack-bot:
    build: .
    container_name: xiaolongxia-slack-bot
    env_file:
      - .env
    restart: unless-stopped
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
