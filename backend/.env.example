# ===========================================
# ProgressHub 後端環境變數範本
# ===========================================
# 複製此檔案為 .env 並填入實際值：
#   cp .env.example .env
# ===========================================

# -------------------------------------------
# 應用程式設定
# -------------------------------------------

# 執行環境（development / test / production）
NODE_ENV=development

# 伺服器監聽埠號
PORT=3000

# API 基礎 URL（用於產生完整的 API 路徑）
API_BASE_URL=http://localhost:3000

# 允許的跨域來源（多個來源以逗號分隔）
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:8080

# -------------------------------------------
# 資料庫設定（PostgreSQL）
# -------------------------------------------

# PostgreSQL 使用者名稱
POSTGRES_USER=progresshub

# PostgreSQL 密碼（請設定強密碼，生產環境必填）
POSTGRES_PASSWORD=your-secure-password-here

# PostgreSQL 資料庫名稱
POSTGRES_DB=progresshub

# PostgreSQL 對外埠號（Docker 映射用）
POSTGRES_PORT=5432

# Prisma 資料庫連線字串
# 格式：postgresql://<使用者>:<密碼>@<主機>:<埠號>/<資料庫名稱>
DATABASE_URL=postgresql://progresshub:your-secure-password-here@localhost:5432/progresshub

# -------------------------------------------
# JWT 認證設定
# -------------------------------------------

# JWT 簽署密鑰（生產環境必須設定，建議至少 32 字元的隨機字串）
# 可用以下指令產生：openssl rand -base64 32
JWT_SECRET=your-jwt-secret-here

# JWT Token 過期時間（格式：數字 + 單位，例如 7d = 7 天、24h = 24 小時）
JWT_EXPIRES_IN=7d

# -------------------------------------------
# Slack OAuth 整合設定
# -------------------------------------------
# 從 Slack API Dashboard 取得：https://api.slack.com/apps

# Slack 應用程式的 Client ID
SLACK_CLIENT_ID=your-slack-client-id

# Slack 應用程式的 Client Secret
SLACK_CLIENT_SECRET=your-slack-client-secret

# Slack 應用程式的 Signing Secret（用於驗證 Slack 請求簽章）
SLACK_SIGNING_SECRET=your-slack-signing-secret

# Slack Bot Token（以 xoxb- 開頭）
SLACK_BOT_TOKEN=xoxb-your-slack-bot-token

# -------------------------------------------
# Docker Compose 專用設定（僅在使用 docker-compose 時需要）
# -------------------------------------------

# Backend 服務對外埠號
# BACKEND_PORT=3000
