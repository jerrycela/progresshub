{
  "$schema": "https://schema.zeabur.app/zeabur.json",
  "build": {
    "command": "npm install && npm run prisma:generate && npm run build"
  },
  "start": {
    "command": "npm run prisma:migrate deploy && npm start"
  }
}
