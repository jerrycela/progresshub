# Consolidated Code Review

Date: 2026-03-09  
Repository: `/Users/admin/progresshub_claude`

## Findings (Deduplicated, Sorted by Severity)

### High

1. **File path and line:** `backend/src/routes/milestones.ts:103`  
   **Severity:** high  
   **Category:** Diff  
   **Description:** `PUT /api/milestones/:id` enforces role checks but does not enforce resource-level authorization for the milestone's owning project. This creates a broken object authorization (IDOR) risk where a privileged user can update milestones outside allowed project scope.  
   **Suggested fix:** After loading the milestone, verify caller access against `existing.projectId` (membership/ownership policy) before update; return `403` on violation.

2. **File path and line:** `packages/frontend/src/services/milestoneService.ts:24`  
   **Severity:** high  
   **Category:** Diff  
   **Description:** `MockMilestoneService.updateMilestone()` returns `Partial<MilestoneData>` cast as `MilestoneData`, which can omit required fields and corrupt downstream state in mock mode.  
   **Suggested fix:** Resolve existing milestone by id, merge patch safely, and return a fully populated `MilestoneData` object (or error if not found).

### Medium

3. **File path and line:** `backend/src/routes/milestones.ts:116`  
   **Severity:** medium  
   **Category:** Diff  
   **Description:** Update validation allows blank `name` values (`optional().isString().trim()`), while create flow enforces non-empty length. This permits invalid empty milestone names on update.  
   **Suggested fix:** Align update validator with create validator, e.g. `optional().isString().trim().isLength({ min: 1, max: 200 })`.

4. **File path and line:** `backend/src/services/milestoneService.ts:56`  
   **Severity:** medium  
   **Category:** Diff  
   **Description:** Newly added milestone update flow lacks explicit regression tests for update behavior and negative paths.  
   **Suggested fix:** Add backend tests for success/validation/not-found/unauthorized update scenarios, plus frontend tests for edit submit, cancel/reset behavior, and failed update UX.

5. **File path or directory:** `/Users/admin/progresshub_claude` (root and major subdirectories)  
   **Severity:** medium  
   **Category:** Holistic  
   **Description:** Agent-operating guidance coverage is incomplete across major working areas. Missing scoped `AGENTS.md` files in major directories weakens consistency for autonomous edits, conventions, and test execution expectations.  
   **Suggested fix:** Add scoped `AGENTS.md` files at root and key subdirectories, documenting ownership boundaries, coding/testing conventions, and pitfalls; keep `CLAUDE.md` symlinked where compatibility is needed.

### Low

6. **File path and line:** `backend/src/routes/milestones.ts:137`  
   **Severity:** low  
   **Category:** Diff  
   **Description:** Route returns raw string error codes (`"MILESTONE_NOT_FOUND"`, `"MILESTONE_UPDATE_FAILED"`) instead of centralized constants, reducing consistency and increasing typo risk.  
   **Suggested fix:** Add these codes to shared `ErrorCodes` and reference constants in route responses.

7. **File path and line:** `README.md:132`, `README.md:135`, `.env.example:35`, `.env.example:45`, `backend/src/config/env.ts:65`, `packages/frontend/src/services/api.ts:16`  
   **Severity:** low  
   **Category:** Holistic  
   **Description:** Environment variable naming appears inconsistent between docs and runtime usage (`BACKEND_PORT`/`VITE_API_URL` vs `PORT`/`VITE_API_BASE_URL`), which can cause onboarding and deployment confusion.  
   **Suggested fix:** Standardize env variable names across docs and code; if aliases are supported, document migration and precedence clearly.

8. **File path or directory:** `/Users/admin/progresshub_claude` (root; QA artifacts)  
   **Severity:** low  
   **Category:** Holistic  
   **Description:** Ad-hoc QA scripts and screenshot artifacts in root and frontend subpaths reduce repository navigability and increase maintenance overhead.  
   **Suggested fix:** Move these to dedicated directories (for example `tools/qa/` and `artifacts/qa/`), define retention conventions, and update docs/.gitignore accordingly.

## Summary

- **Total issues:** 8
- **By severity:** high 2, medium 3, low 3, critical 0
- **Agents run:**
  - AGENT 1: Diff Review (uncommitted + recent committed changes)
  - AGENT 2: Holistic Review (project structure and agent readiness)
- **Deduplication note:** No direct duplicates between Diff and Holistic findings; items were normalized and severity-sorted.
- **Overall assessment:** Significant authorization and correctness risks exist in recent milestone update changes and should be fixed before merge. Structural documentation/organization issues are non-blocking but worth addressing to improve long-term maintainability and agent reliability.
